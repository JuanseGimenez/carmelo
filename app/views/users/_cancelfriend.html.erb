<%= form_for(current_user.friendships.find_by(friend_id: @user.id), html: {method: :delete}, remote: true) do |f| %>
  <div><%= hidden_field_tag :user_id, @user.id %></div>
  <%= f.submit "Cancelar", class: "btn btn-warning text-uppercase" %>
<% end %>